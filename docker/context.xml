<Context>
    <Parameter name="online.kheops.pacs.uri" value="${kheops_pacs_url}"
               override="false"/>
    <Parameter name="online.kheops.auth_server.uri" value="${kheops_authorization_url}"
               override="false"/>
</Context>
